// Exercise 26
// Create a new index26.js file
// We need to define Winnipeg using variables and all the data types that we learned
// Declare at least 6 variables that will help you describe the city
// Try using at least one variable of each type
// Show each value using console.log() with the variable name (string) as first parameter and the actual variable value as second
// Example: console.log('name', name);
// Also show the variable names and the type of value it has assigned (use typeof)

var cityName = 'Winnipeg';
var countryLocation = 'Canada';
var population = 123764172;
var locatedOnEarth = true;
var hasAliens = undefined;
var hasSuperHeroes = null;


console.log('City Name:', cityName, typeof(cityName));
console.log('Country Location:', countryLocation, typeof(countryLocation));
console.log('Population:', population, typeof(population));
console.log('Located on Earth:', locatedOnEarth, typeof(locatedOnEarth));
console.log('Has Aliens:', hasAliens, typeof(hasAliens));
console.log('Has Super Heroes:', hasSuperHeroes, typeof(hasSuperHeroes));
